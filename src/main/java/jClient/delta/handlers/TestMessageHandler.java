package jClient.delta.handlers;

import jClient.base.MessageHandler;

public class TestMessageHandler implements MessageHandler {

    @Override
    public String handleMessage(String message) {
        return message;
    }
}
